# Development Environment Configuration
# Safe defaults for local development and testing

# Cost Control Flags (DEFAULT: DISABLED for development)
# Videos and images are DISABLED by default to prevent accidental API costs
# Use placeholder URIs instead for fast, free testing

ENABLE_VIDEOS=false
ENABLE_IMAGES=false

# To enable video generation during development (costs ~$0.20 per 5-second video):
# ENABLE_VIDEOS=true

# To enable image generation during development (costs ~$0.03 per image):
# ENABLE_IMAGES=true

# Note: GCP credentials are typically configured via:
# - Application Default Credentials (gcloud auth application-default login)
# - Service account key file (GOOGLE_APPLICATION_CREDENTIALS environment variable)
# - Cloud Run/Cloud Functions automatic credentials (in production)
