# ============================================================================
# BrandMind AI - Environment Variables Template
# ============================================================================
# Copy this file to .env and fill in your actual API keys
# Command: cp backend/.env.example backend/.env
# ============================================================================

# ============================================================================
# CORE AI SERVICES
# ============================================================================

# Claude Sonnet 4.5 (temporary - will be replaced with Gemini later)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-...

# Gemini 3.0 Pro (primary AI service for strategy and content generation)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# AGI API (for intelligent web research and competitor analysis)
# Get your API key from: https://agi.tech/
AGI_API_KEY=your-agi-api-key

# MiniMax (for image and video generation)
# Get your API key from: https://www.minimaxi.com/
MINIMAX_API_KEY=your-minimax-api-key

# ============================================================================
# DATABASE & STORAGE
# ============================================================================

# Convex Database (for storing campaign data)
# Get your deployment URL from: https://dashboard.convex.dev/
CONVEX_URL=https://example-lion-123.convex.cloud

# Cloudflare R2 Object Storage (S3-compatible)
# Get credentials from: https://dash.cloudflare.com/
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key
R2_SECRET_ACCESS_KEY=your-r2-secret-key
R2_BUCKET=superscrat

# ============================================================================
# SOCIAL MEDIA & BUSINESS APIs (OPTIONAL)
# ============================================================================
# NOTE: These tokens are OPTIONAL. If businesses don't have API access,
# the system will use AGI API as a fallback to scrape public data from:
# - Google Maps (public reviews)
# - Yelp
# - TripAdvisor
# - Public social media profiles
#
# Providing these tokens enables richer data but is NOT required for demo.
# ============================================================================

# Google My Business API (for claimed business reviews/photos)
# Get API key from: https://console.cloud.google.com/
# NOTE: Only works for claimed businesses. Unclaimed businesses use AGI fallback.
GOOGLE_MY_BUSINESS_API_KEY=your-gmb-api-key

# Facebook Marketing API (for post insights)
# Get access token from: https://developers.facebook.com/
# OPTIONAL: If unavailable, agent will skip past performance analysis
FACEBOOK_ACCESS_TOKEN=your-facebook-token

# Instagram Graph API (for engagement metrics)
# Get access token from: https://developers.facebook.com/docs/instagram-api
# OPTIONAL: If unavailable, agent will skip past performance analysis
INSTAGRAM_ACCESS_TOKEN=your-instagram-token

# Google Trends API (for location-based trend data)
# Get API key from: https://console.cloud.google.com/
# OPTIONAL: If unavailable, agent will rely on AGI API market research
GOOGLE_TRENDS_API_KEY=your-trends-api-key

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================

# FastAPI server port
PORT=8080

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================================================
# IMPORTANT NOTES
# ============================================================================
#
# 1. NEVER commit the .env file to git (already in .gitignore)
#
# 2. For local development, copy this file:
#    cp backend/.env.example backend/.env
#
# 3. Required for basic functionality:
#    - ANTHROPIC_API_KEY or GEMINI_API_KEY (at least one)
#    - AGI_API_KEY (critical for web research)
#    - MINIMAX_API_KEY (for content generation)
#    - CONVEX_URL (for data storage)
#    - R2 credentials (for media storage)
#
# 4. Optional but recommended:
#    - GOOGLE_MY_BUSINESS_API_KEY (better review data if business is claimed)
#    - FACEBOOK_ACCESS_TOKEN (enables past performance analysis)
#    - INSTAGRAM_ACCESS_TOKEN (enables engagement insights)
#    - GOOGLE_TRENDS_API_KEY (enhances market trend analysis)
#
# 5. AGI API Fallback Strategy:
#    When social media tokens are unavailable, AGI API will automatically:
#    - Scrape public Google Maps reviews
#    - Analyze competitor social media posts
#    - Extract market trends from web search
#    This ensures the demo works even without business API access!
#
# ============================================================================
